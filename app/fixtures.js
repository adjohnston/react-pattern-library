const fixtures = {
  Button: [{
    header: 'A basic button',
    props: {
      className: 'primary',
      children: 'Button'
    },
  }],

  Tabbable: [{
    header: 'Tabbing',
    props: {
      className: 'basic'
    }
  }]
}

export default fixtures
